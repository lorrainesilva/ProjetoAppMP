<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mp20172.easyshop">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".SignUpActivity"
            android:label="@string/title_activity_sign_up" />
        <activity
            android:name=".ForgotPasswordActivity"
            android:label="@string/title_activity_forgot_password" />
        <activity
            android:name=".AllItemsActivity"
            android:label="@string/title_activity_all_items" />
        <activity
            android:name=".AllItemsOfListActivity"
            android:label="@string/title_activity_all_items_of_list_example" />
        <activity
            android:name=".StoreListActivity"
            android:label="@string/title_activity_store_list" />
        <activity
            android:name=".PurchasedItemsActivity"
            android:label="@string/title_activity_purchase_items" />
        <activity
            android:name=".RegisterItemActivity"
            android:label="@string/title_activity_register_item"/>
    </application>

</manifest>